import {
  Directory,
  categories,
} from '../../components/item-directory/directory-render.component';

const Home = () => {
  return <Directory categories={categories} />;
};

export default Home;
